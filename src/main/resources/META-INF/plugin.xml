<!-- Plugin Configuration File. Read more: https://plugins.jetbrains.com/docs/intellij/plugin-configuration-file.html -->
<idea-plugin>
    <id>dev.gaphunter.cassandracqlsessionreusecompanion</id>
    <name>Cassandra CqlSession Reuse Companion</name>
    <vendor>Gap Hunter Labs</vendor>
    <description><![CDATA[
        <p>Gutter warning icon on a <code>CqlSession.builder()....build()</code>
        construction chain (DataStax Java driver for Apache Cassandra)
        written inside a regular method body -- DataStax's own best
        practices documentation states: "These root objects are
        expensive to create because they initialize and maintain
        connection pools to every node in a cluster... Create one
        session instance for each application, and then reuse that
        session for the entire lifetime of the application."</p>
        <p><b>100% static text/PSI analysis</b> -- matches the builder
        chain by simple text, so it works whether the real DataStax
        driver jar is on the classpath or not. Java and Kotlin.
        Confirmed gap: JetBrains supports Cassandra only via Database
        Tools (data/CQL browsing), and the one third-party Cassandra
        plugin found is a syntax-highlighting tool -- neither checks
        CqlSession reuse in application code.</p>
        <p>Stated honestly: v0.1 only flags the direct
        <code>CqlSession.builder()....build()</code> chain. Never flags
        a session obtained by reference from an existing shared
        instance/dependency injection, or a call inside a constructor
        (legitimate "create once" locations).</p>
        <p>Free. No license nags, no telemetry, no network calls.</p>
    ]]></description>

    <depends>com.intellij.modules.platform</depends>
    <depends>com.intellij.java</depends>
    <depends>org.jetbrains.kotlin</depends>

    <extensions defaultExtensionNs="org.jetbrains.kotlin">
        <supportsKotlinPluginMode supportsK1="true" supportsK2="true"/>
    </extensions>

    <extensions defaultExtensionNs="com.intellij">
        <codeInsight.lineMarkerProvider language="JAVA"
            implementationClass="dev.gaphunter.cassandracqlsessionreusecompanion.gutter.ClientBuiltPerCallLineMarkerProvider"/>
        <codeInsight.lineMarkerProvider language="kotlin"
            implementationClass="dev.gaphunter.cassandracqlsessionreusecompanion.gutter.ClientBuiltPerCallLineMarkerProvider"/>
        <notificationGroup id="Cassandra CqlSession Reuse Companion" displayType="BALLOON"/>
    </extensions>
</idea-plugin>
